
  <% content_for :page_header do %>
    Editing Ticket
  <% end %>
  <% content_for :page_actions do %>
  	<% if params[:client_id] %>
	    <%= link_to admin_client_ticket_path(params[:client_id], @ticket), :class => 'btn btn-primary btn-labeled', :style=>"width: 100%;" do %>
	                <span class="btn-label icon fa fa-plus"></span>Back
	    <% end %>
    <% else %>
	     <%= link_to admin_ticket_path(@ticket), :class => 'btn btn-primary btn-labeled', :style=>"width: 100%;" do %>
	                <span class="btn-label icon fa fa-plus"></span>Back
	    <% end %>
	<% end %>

  <% end %>

<%= render 'admin/header' %>

<%= render 'form' %>
