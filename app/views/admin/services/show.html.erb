  <% content_for :page_header do %>
   <%=@service.name %>
  <% end %>
  <% content_for :page_actions do %>
   <%= link_to admin_services_path, :class => 'btn btn-primary btn-labeled', :style=>"width: 100%;" do %>
                <span class="btn-label icon fa fa-chevron-left"></span>Back
    <% end %>
  <% end %>
<%= render 'admin/header' %>


<div class="panel colourable">
          <div class="panel-heading">
            <span class="panel-title">Service Details</span>
            <div class="panel-heading-controls">
              <span class="panel-heading-text"><em><%= link_to "Public View", service_path %></em>&nbsp;&nbsp;<span style="color: #ccc">|</span>&nbsp;&nbsp;</span>
               <%= link_to edit_admin_service_path(@service), :class => 'btn btn-xs' do %>
                <span class="btn-label icon fa fa-plus"></span>Edit
               <% end %>
            </div>
          </div> <!-- / .panel-heading -->
          <div class="panel-body">
            <div class="row">
              <div class="col-md-8">
                <div>
                  <h3> Service Header </h3>
                  <%= image_tag @service.header.url(:large) %>
                </div>
              <div>
                  <h3> Service Content </h3>
                  <%= sanitize(@service.description) %>
              </div>
              </div>
              <div class="col-md-4" id="sidebar">
               <h3> Service Logo </h3>
                <%= link_to 'Crop', crop_admin_service_path(@service), :remote => true, :method => :post %></td>
                <%= image_tag @service.logo(:medium) %>
              </div>
            </div>
          </div>
