
<% if params[:gallery_id] %>
<%= form_for [:admin, @gallery, @photo] do |f| %>
	<%= f.file_field :file %>
	<%= f.submit :action %>
<% end %>

<% else %> 

<%= form_for [:admin, @photo] do |f| %>
	<%= f.file_field :file %>
	<%= f.submit :action %>
<% end %>

<% end %>