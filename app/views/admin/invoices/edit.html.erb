<script>
  var init = [];
  init.push(function () {
    $('body').addClass('mmc page-profile');
  });
</script>

  <% content_for :page_header do %>
    Editing Invoice
  <% end %>
  <% content_for :page_actions do %>
    <%= link_to admin_invoice_path(@invoice), :class => 'btn btn-primary btn-labeled', :style=>"width: 100%;" do %>
                <span class="btn-label icon fa fa-plus"></span>Back
    <% end %>
  <% end %>

<%= render 'admin/header' %>

<%= render 'form' %>
